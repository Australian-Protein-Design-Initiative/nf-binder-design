profiles {
    local {
        executor {
            name = 'local'
            
            // We only run one task at a time, so the GPU only gets one task at a time.
            queueSize = 1
            pollInterval = '30 sec'
        }
        process {
            executor = 'local'
            stageInMode = 'symlink'
            maxRetries = 0
            cache = 'lenient'
        }
    }
}
