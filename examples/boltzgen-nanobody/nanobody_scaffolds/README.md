There are the BoltzGen example nanobody scaffolds, but converted to PDB format, all using chain B.

This means we can treat the target as chain A consistently in the config (and not worry about chain ID confusion when using constraints etc).

3eak (h-NbBcII10FGLA) is not in the Boltzgen example and has been added as an extra scaffold.
